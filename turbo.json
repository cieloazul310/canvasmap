{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["lib", "module"]
    },
    "dev": {
      "dependsOn": ["^dev"],
      "outputs": ["lib", "module"]
    },
    "clean": {}
  }
}
