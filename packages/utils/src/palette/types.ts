export type Palette = {
  background: {
    main: string;
    contrast: string;
  };
  boundary: {
    pref: string;
    town: string;
  };
  waterarea: string;
  railway: {
    section: string;
    station: string;
  };
  road: {
    national: string;
    highway: string;
    base: string;
  };
  building: string;
  contour: string;
  label: {
    terrain: string;
    water: string;
    em: string;
    base: string;
  };
};
